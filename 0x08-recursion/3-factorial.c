#include "main.h"
#include <stdio.h>

/**
 * factorial - ll
 * @n: ll
 * Return: fac
 */

int factorial(int n)
{
	if (n < 0)
		return (-1);
	if (n == 0)
		return (1);
	if (n == 1)
		return (n * factorial(n -1);
}
